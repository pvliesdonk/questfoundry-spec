# Intent Handler — tu.open (Scaffold)

Inputs
- Envelope with `intent = tu.open`, Hot context, loop set.

Process
- Create TU context, initialize session, acknowledge.

Outputs
- `ack` to sender; internal state initialized.

