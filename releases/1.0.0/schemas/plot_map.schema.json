{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liesdonk.nl/questfoundry/v1.0.0/schemas/plot_map.schema.json",
  "title": "Plot Map Graph",
  "type": "object",
  "required": ["version","nodes", "edges"],
  "additionalProperties": false,
  "properties": {
    "version": {"type": "string", "const": "1.0.0"},
    "nodes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "kind"],
        "properties": {
          "id": {"type": "string"},
          "kind": {"type": "string", "enum": ["beat", "choice", "rejoin"]}
        }
      }
    },
    "edges": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["from", "to", "kind"],
        "properties": {
          "from": {"type": "string"},
          "to": {"type": "string"},
          "kind": {"type": "string", "enum": ["progress", "choice", "rejoin"]}
        }
      }
    },
    "invariants": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "dead_ends_allowed": {"type": "boolean", "default": false},
        "max_soft_rejoin_distance": {"type": "integer"}
      }
    }
  }
}
