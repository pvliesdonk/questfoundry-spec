{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liesdonk.nl/questfoundry/v1.0.0/schemas/claim_registry.schema.json",
  "title": "Claim Registry",
  "type": "object",
  "additionalProperties": false,
  "required": ["version","claims"],
  "properties": {
    "version": {"type": "string", "const": "1.0.0"},
    "claims": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "text", "support"],
        "properties": {
          "id": {"type": "string"},
          "text": {"type": "string"},
          "support": {"type": "string", "enum": ["factual", "genre", "invented-but-consistent"]},
          "confidence": {"type": "number", "minimum": 0, "maximum": 1},
          "sources": {"type": "array", "items": {"type": "string"}},
          "status": {"type": "string", "enum": ["approved", "rejected", "pending"]}
        }
      }
    }
  }
}
