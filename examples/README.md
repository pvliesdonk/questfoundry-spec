# Examples (v1.0.0 snapshot)

This folder contains:
- `contracts/` — legacy annotated examples from the v1.0.0 snapshot (see README inside; new ones incoming).
- `workflow/` — placeholder until refreshed end-to-end runs are generated.
